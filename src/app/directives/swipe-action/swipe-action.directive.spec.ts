import { SwipeActionDirective } from './swipe-action.directive';

describe('SwipeActionDirective', () => {
  it('should create an instance', () => {
    const directive = new SwipeActionDirective();
    expect(directive).toBeTruthy();
  });
});
